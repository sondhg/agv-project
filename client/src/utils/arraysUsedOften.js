export const startPoints = Array.from({ length: 15 }, (_, index) => index + 1); //from 1 to 15

export const endPoints = Array.from({ length: 15 }, (_, index) => index + 1); //from 1 to 15

export const loadNames = ["Stone", "Cement", "Steel", "Iron"];
